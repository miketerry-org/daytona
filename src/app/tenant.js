// tenant.js:

"use strict";

// load all necessary modules
const Base = require("./base");
const Configurable = require("./configurable");

class tenant extends Configurable {}

module.exports = tenant;
